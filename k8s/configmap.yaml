apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service
  labels:
    app: user-service
data:
  SERVER_HOST: "0.0.0.0"
  SERVER_PORT: "8000"
  SERVER_RELOAD: "false"
  LOG_LEVEL: "INFO"
  CACHE_TTL_SECONDS: "10"
  APP_TITLE: "User Service"
